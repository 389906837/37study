package ${info.servicePackage};

import ${info.modelPackage}.${info.modelName};
import com.cang.os.common.service.BaseService;

public interface ${info.modelName}Service extends BaseService<${info.modelName}> {
 
    
}
